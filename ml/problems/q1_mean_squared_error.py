# ml/problems/q1_mean_squared_error.py
import numpy as np

def mean_squared_error(y_true, y_pred):
    """
    Compute the Mean Squared Error (MSE) between true and predicted values.

    Formula:
        MSE = (1/n) * Σ (y_true[i] - y_pred[i])²

    Args:
        y_true: numpy array of shape (n,)
        y_pred: numpy array of shape (n,)

    Returns:
        float: the MSE value
    """
    pass
