# ml/problems/q2_linear_regression_predict.py
import numpy as np

def predict_linear(X, w, b):
    """
    Predict outputs using a linear model.

    Formula:
        y_pred = X @ w + b

    Args:
        X: numpy array of shape (n_samples, n_features)
        w: numpy array of shape (n_features,)
        b: float

    Returns:
        y_pred: numpy array of shape (n_samples,)
    """
    pass
