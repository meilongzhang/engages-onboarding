# dl/problems/q2_simple_nn.py
import torch.nn as nn

def create_simple_nn(input_dim, hidden_dim, output_dim):
    """
    Create a neural network with:
        - Linear layer (input_dim → hidden_dim)
        - ReLU
        - Linear layer (hidden_dim → output_dim)

    Args:
        input_dim: int
        hidden_dim: int
        output_dim: int

    Returns:
        nn.Module
    """
    pass
